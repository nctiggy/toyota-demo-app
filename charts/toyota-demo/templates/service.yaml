apiVersion: v1
kind: Service
metadata:
  name: {{ include "toyota-demo.fullname" . }}
  labels:
    {{- include "toyota-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "toyota-demo.selectorLabels" . | nindent 4 }}
