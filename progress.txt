# Toyota Demo App — Progress Log

## US-001: PostgreSQL Database Setup ✓
- Configured listen_addresses='*' in postgresql.conf
- Added remote access rule (host all all 0.0.0.0/0 md5) to pg_hba.conf
- Set postgres password to 'postgres'
- Created toyota database
- Created vehicles table and seeded 10 Toyota vehicles
- Restarted PostgreSQL
- Verified: remote TCP connectivity works, SELECT count(*) FROM vehicles returns 10

## US-002: Complete Application ✓
- Created package.json with Express 4.21 and pg 8.13
- Built server.js: single-file Express app with Toyota-branded embedded HTML/CSS
- Endpoints: GET / (HTML), GET /healthz (200 OK), GET /api/status (DB status JSON), GET /api/vehicles (vehicle data)
- Toyota branding: #EB0A1E red, #58595B gray, Montserrat font, TOYOTA logo, version badge
- v1/v2 differentiation via APP_VERSION and CONNECTION_METHOD env vars
- Connection flow visualization, vehicle table, status card with latency
- v2 mode adds Migration Complete card and Migration Stats
- Created .dockerignore
- Validated: all 4 endpoints return correct data, 10 vehicles, Toyota styling confirmed

## US-003: Dockerfile ✓
- Created multi-stage Dockerfile with node:20-alpine
- Image size: ~49MB (under 100MB requirement)
- Local build: docker build -t toyota-demo-app:test . succeeds
- Container run: docker run -e DB_HOST=10.30.0.27 -p 3000:3000 toyota-demo-app:test serves app
- Verified: /healthz OK, /api/status connected=true, /api/vehicles returns 10 vehicles

## US-004: GitHub Repo + CI ✓
- Created repo: github.com/nctiggy/toyota-demo-app (public)
- Set DOCKERHUB_USERNAME and DOCKERHUB_TOKEN secrets from 1Password docker(PAT)
- Created .github/workflows/docker.yml for multi-arch builds on tag push
- Pushed code and tag v1.0.0
- GitHub Actions completed successfully in 57s
- Docker Hub has nctiggy/toyota-demo-app:1.0.0 with both amd64 and arm64 platforms

## US-005: Helm Chart ✓
- Created charts/toyota-demo/ with Chart.yaml, values.yaml, values-v1.yaml, values-v2.yaml
- Templates: deployment, service, configmap, secret, _helpers.tpl, NOTES.txt
- Deployment uses envFrom (configmap + secret), liveness/readiness probes on /healthz
- Service: NodePort on 30080
- helm lint passes for default, v1, and v2 value files
- helm template renders correct v1 config (direct IP) and v2 config (K8s service DNS)
